---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: spotify-to-plex
  namespace: self-hosted
spec:
  interval: 1h
  path: ./kubernetes/apps/self-hosted/spotify-to-plex/app
  postBuild:
    substitute:
      APP: spotify-to-plex
    substituteFrom:
      - kind: Secret
        name: cluster-secrets
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: self-hosted
  wait: false
