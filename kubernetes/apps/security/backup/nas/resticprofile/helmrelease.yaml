---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nas-resticprofile
spec:
  chartRef:
    kind: OCIRepository
    name: nas-resticprofile
  interval: 1h
  values:
    controllers:
      backup:
        type: cronjob
        cronjob:
          schedule: 0 1 * * 6
          timeZone: Europe/Berlin
          backoffLimit: 0
          concurrencyPolicy: Forbid
          successfulJobsHistory: 1
          failedJobsHistory: 1
          ttlSecondsAfterFinished: 18000
        containers:
          app:
            image:
              repository: ghcr.io/creativeprojects/resticprofile
              tag: 0.33.1@sha256:c34d15a2a2c5a88029f05d941c518680d8a29d474f70b6d7eb5379996dac1424
            envFrom: &envFrom
              - secretRef:
                  name: nas-resticprofile-secret
            env:
              TZ: Europe/Berlin
            args:
              - -c
              - /resticprofile/profiles.yaml
              - --profile
              - nas
              - backup
        pod:
          restartPolicy: Never
      check:
        type: cronjob
        cronjob:
          schedule: 0 3 1 * *
          timeZone: Europe/Berlin
          backoffLimit: 0
          concurrencyPolicy: Forbid
          successfulJobsHistory: 1
          failedJobsHistory: 1
          ttlSecondsAfterFinished: 18000
        containers:
          app:
            image:
              repository: ghcr.io/creativeprojects/resticprofile
              tag: 0.33.1@sha256:c34d15a2a2c5a88029f05d941c518680d8a29d474f70b6d7eb5379996dac1424
            envFrom: *envFrom
            env:
              TZ: Europe/Berlin
            args:
              - -c
              - /resticprofile/profiles.yaml
              - --profile
              - nas
              - check
        pod:
          restartPolicy: Never
    persistence:
      key-file:
        type: secret
        name: nas-resticprofile-secret
        globalMounts:
          - path: /resticprofile/backup-key
            subPath: backup-key
      profiles-file:
        type: secret
        name: nas-resticprofile-secret
        globalMounts:
          - path: /resticprofile/profiles.yaml
            subPath: profiles.yaml
      nas:
        type: nfs
        server: nas.internal
        path: /nas
        globalMounts:
          - path: /nas/home-assistant
            subPath: home-assistant
          - path: /nas/photos
            subPath: photos
          - path: /nas/paperless
            subPath: paperless

