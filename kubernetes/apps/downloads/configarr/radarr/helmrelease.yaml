---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: configarr-radarr
spec:
  chartRef:
    kind: OCIRepository
    name: configarr-radarr
  interval: 1h
  values:
    controllers:
      configarr-radarr:
        type: cronjob
        cronjob:
          schedule: 0 0 * * 1
          timeZone: Europe/Berlin
          backoffLimit: 0
          concurrencyPolicy: Forbid
          successfulJobsHistory: 1
          failedJobsHistory: 1
          ttlSecondsAfterFinished: 18000
        containers:
          app:
            image:
              repository: ghcr.io/raydak-labs/configarr
              tag: 1.30@sha256:ec585b6d2530f6090ee26fdcc9701f279666c24dc96c9087208e88ec867c2416
            envFrom:
              - secretRef:
                  name: "{{ .Release.Name }}-secret"
            env:
              TZ: Europe/Berlin
        pod:
          restartPolicy: Never
    persistence:
      app-data:
        type: persistentVolumeClaim
        size: 1Gi
        storageClass: local-hostpath
        accessMode: ReadWriteOnce
        globalMounts:
          - path: /app/repos
      config:
        type: configMap
        name: configarr-radarr-configmap
        globalMounts:
          - path: /app/config/config.yml
            subPath: config.yml
            readOnly: true
