---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: gatus
spec:
  dataFrom:
    - extract:
        key: gatus
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: gatus-secret
    template:
      data:
        BUDDY_HEARTBEAT_TOKEN: "{{ .BUDDY_HEARTBEAT_TOKEN }}"
        BUDDY_PUSHOVER_TOKEN: "{{ .BUDDY_PUSHOVER_TOKEN }}"
        BUDDY_PUSHOVER_USER_KEY: "{{ .BUDDY_PUSHOVER_USER_KEY }}"
        BUDDY_STATUS_HOSTNAME: "{{ .BUDDY_STATUS_HOSTNAME }}"
        GATUS_ALERTING_DISCORD_WEBHOOK_URL: "{{ .ALERT_DISCORD_WEBHOOK_URL }}"
