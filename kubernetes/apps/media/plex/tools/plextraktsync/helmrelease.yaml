---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: plextraktsync
spec:
  chartRef:
    kind: OCIRepository
    name: plextraktsync
  interval: 1h
  values:
    controllers:
      plextraktsync:
        type: cronjob
        cronjob:
          schedule: 0 6,18 * * *
          backoffLimit: 0
          concurrencyPolicy: Forbid
          successfulJobsHistory: 1
          failedJobsHistory: 1
          ttlSecondsAfterFinished: 18000
          timeZone: Europe/Berlin
        containers:
          app:
            image:
              repository: ghcr.io/taxel/plextraktsync
              tag: 0.35.18@sha256:ccee7b4c5d150a18e23dfd189c0c5ddec388a10f974a319fdc0c7e39a4bec52f
            envFrom:
              - secretRef:
                  name: plextraktsync-secret
            env:
              TZ: Europe/Berlin
            args:
              - sync
        pod:
          restartPolicy: Never
    persistence:
      config-file:
        type: secret
        name: plextraktsync-secret
        globalMounts:
          - path: /app/config/config.yml
            subPath: config.yml
      servers-file:
        type: secret
        name: plextraktsync-secret
        globalMounts:
          - path: /app/config/servers.yml
            subPath: servers.yml
      pytrakt-file:
        type: secret
        name: plextraktsync-secret
        globalMounts:
          - path: /app/config/.pytrakt.json
            subPath: .pytrakt.json
